msg: Welcome to Amazon
server:
  port: 8081
  
spring:
  application:
    name: AMAZON-API
    
  boot:
    admin:
      client:
        url: http://localhost:2222
eureka:
  client:
    service-url:
       defaultZone: http://localhost:8761/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'
